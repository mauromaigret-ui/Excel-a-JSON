# Frontend (local)

Interfaz web en navegador (localhost). Se conecta al backend local.

## Inicio rápido
El frontend está servido por el backend. Abre `http://localhost:8000` luego de iniciar FastAPI.
